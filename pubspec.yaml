name: appwrite_functions_context
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo
environment:
  sdk: '>=2.19.0 <4.0.0'

publish_to: none

# Add regular dependencies here.
dependencies:
  dart_appwrite: ^16.0.0
# path: ^1.8.0
dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
